echo     stream  tcp    nowait  root    internal
echo     dgram   udp    wait    root    internal
daytime  stream  tcp    nowait  root    internal
daytime  dgram   udp    wait    root    internal
time     stream  tcp    nowait  root    internal
time     dgram   udp    wait    root    internal

# <service_name> <sock_type> <proto> <flags> <user> <server_path> <args>
21	stream	tcp	nowait	root	ftpd  ftpd -w /home/ftp_dir	
#ftp     stream  tcp     nowait  root    /usr/sbin/ftpd          /usr/sbin/ftpd
#telnet	stream  tcp 	nowait  root    /usr/sbin/telnetd	/usr/bin/telnetd -l /bin/login

#telnet	stream  tcp 	nowait  root    /usr/sbin/telnetd	/usr/bin/tcpd telnetd

23	stream  tcp 	nowait  root    /usr/sbin/telnetd	/usr/bin/telnetd -l /bin/login
